import { NextRequest, NextResponse } from "next/server";
export async function GET(request:NextRequest){const q=request.nextUrl.searchParams.get("q")?.trim();if(!q||q.length<2)return NextResponse.json({results:[]});try{const response=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(q)}&count=8&language=zh&format=json`);if(!response.ok)throw new Error("provider");const raw=await response.json() as {results?:Array<Record<string,string|number>>};const results=(raw.results??[]).map(x=>({id:`${x.id}`,name:x.name,admin1:x.admin1,country:x.country,latitude:x.latitude,longitude:x.longitude,timezone:x.timezone}));return NextResponse.json({results},{headers:{"Cache-Control":"public, max-age=600"}})}catch{return NextResponse.json({results:[],error:true},{status:502})}}
